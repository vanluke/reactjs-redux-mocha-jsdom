import {expect} from 'chai';

//import server from '../server';

describe('server', () => {
  it('start at server at localhost 8090', () => {
    //  const srv = server({ subscribe: function (){} });
      // expect(srv).to.not.equal(undefined);
  });
});
