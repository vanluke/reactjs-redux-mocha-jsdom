import {Map} from 'immutable';
import setEntries from './logic/setEntries';
import next from './logic/next';
import vote from './logic/vote';
const INITIAL_STATE = Map();

export { setEntries, next, vote, INITIAL_STATE};
